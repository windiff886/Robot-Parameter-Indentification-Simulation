# run_experiment 统一实验配置
# robot 可选: panda / piper
robot: "piper"
backend: "sim"

# 以下路径均相对仓库根目录；留空时会按 robot 使用默认值
# controller_config: "config/force_controller_node.yaml"
# sim_config: "config/panda_sim_node.yaml"
# scene: "franka_emika_panda/scene.xml"
# collision_model: "franka_emika_panda/panda.xml"
# hardware_config: "config/piper_real_experiment.yaml"
